Fixture for the document-structure firing tests. `setup-guide.md` is a
setup guide in the shape drafts arrive in: it opens with the history of
the tool, its headings are "Overview", "Details" and "Notes", one
paragraph carries the install and the configuration, the procedure is
a paragraph with three actions in one sentence, the four supported
platforms with their versions and package names are bulleted, the
config file is called "the settings file" in one place and "config"
in another, and "see above" appears twice. `CHANGELOG.md` is a plain,
well-formed file for the scenarios that must not fire.
